# Base URL of the Sentinel agent API.
VITE_API_BASE_URL=https://api.sentinel.example/v1

# Resolve data against the seeded in-browser database instead of the API.
# Defaults to true; set to "false" once the backend endpoints are live.
VITE_USE_MOCK_API=true
